# Generation recipes

Recipes are versioned independently from seed releases, schemas, models, and execution runs. Shared schemas and validators live under `shared/`; framework-specific recipes live under `generation/<method>/<strategy>/recipe_vNN/`.

